diff --git a/src/app/app.routes.ts b/src/app/app.routes.ts
index 5af0da9..8763695 100644
--- a/src/app/app.routes.ts
+++ b/src/app/app.routes.ts
@@ -11,6 +11,11 @@ export const routes: Routes = [
     loadComponent: () =>
       import('./components/writing-page/writing-page').then((m) => m.WritingPage),
   },
+  {
+    path: 'writing/:slug',
+    loadComponent: () =>
+      import('./components/writing-page/blog-post-page/blog-post-page').then((m) => m.BlogPostPage),
+  },
   {
     path: 'inspiration',
     loadComponent: () =>
diff --git a/src/app/components/writing-page/blog-post-page/blog-post-page.css b/src/app/components/writing-page/blog-post-page/blog-post-page.css
new file mode 100644
index 0000000..5e516b8
--- /dev/null
+++ b/src/app/components/writing-page/blog-post-page/blog-post-page.css
@@ -0,0 +1,26 @@
+.blog-post {
+  padding-bottom: 24px;
+}
+
+.back-link {
+  display: inline-block;
+  margin: 20px 0;
+  font-size: 0.9rem;
+  color: var(--muted);
+}
+
+.post-title {
+  font-size: 1.6rem;
+  font-weight: 700;
+  margin: 8px 0 6px;
+  line-height: 1.25;
+}
+
+.post-content {
+  margin-top: 20px;
+  display: grid;
+  gap: 16px;
+  line-height: 1.7;
+  color: var(--fg);
+  opacity: 0.92;
+}
diff --git a/src/app/components/writing-page/blog-post-page/blog-post-page.html b/src/app/components/writing-page/blog-post-page/blog-post-page.html
new file mode 100644
index 0000000..fe7b4a1
--- /dev/null
+++ b/src/app/components/writing-page/blog-post-page/blog-post-page.html
@@ -0,0 +1,18 @@
+<article class="blog-post" *ngIf="post() as p; else notFound">
+  <a class="back-link link-draw" routerLink="/writing">← Back to writing</a>
+
+  <span class="entry-tag">{{ p.tag }}</span>
+  <h1 class="post-title">{{ p.title }}</h1>
+  <span class="entry-date">{{ p.date }}</span>
+
+  <div class="post-content">
+    <p *ngFor="let paragraph of p.content">{{ paragraph }}</p>
+  </div>
+</article>
+
+<ng-template #notFound>
+  <div class="blog-post">
+    <a class="back-link link-draw" routerLink="/writing">← Back to writing</a>
+    <p class="empty-state">Couldn't find that post.</p>
+  </div>
+</ng-template>
diff --git a/src/app/components/writing-page/blog-post-page/blog-post-page.ts b/src/app/components/writing-page/blog-post-page/blog-post-page.ts
new file mode 100644
index 0000000..4a946fc
--- /dev/null
+++ b/src/app/components/writing-page/blog-post-page/blog-post-page.ts
@@ -0,0 +1,25 @@
+import { Component, computed, inject } from '@angular/core';
+import { toSignal } from '@angular/core/rxjs-interop';
+import { CommonModule } from '@angular/common';
+import { ActivatedRoute, RouterLink } from '@angular/router';
+import { map } from 'rxjs';
+import { BLOG_POSTS, BlogPost } from '../../../shared/blog-posts.data';
+
+@Component({
+  selector: 'app-blog-post-page',
+  imports: [CommonModule, RouterLink],
+  templateUrl: './blog-post-page.html',
+  styleUrl: './blog-post-page.css',
+})
+export class BlogPostPage {
+  private readonly route = inject(ActivatedRoute);
+
+  // Reactive to the :slug param via a signal (not a one-off snapshot read) so
+  // this keeps working correctly if the router ever reuses this component
+  // instance while navigating between two different posts.
+  private readonly slug = toSignal(this.route.paramMap.pipe(map((params) => params.get('slug'))));
+
+  protected readonly post = computed<BlogPost | undefined>(() =>
+    BLOG_POSTS.find((p) => p.slug === this.slug()),
+  );
+}
diff --git a/src/app/components/writing-page/writing-page.html b/src/app/components/writing-page/writing-page.html
index 8f4aef1..7789129 100644
--- a/src/app/components/writing-page/writing-page.html
+++ b/src/app/components/writing-page/writing-page.html
@@ -5,6 +5,18 @@
     Blog posts, research notes, and things I've been learning along the way.
   </p>
 
+  <app-divider [title]="'Latest posts'" />
+
+  <ul class="entries" *ngIf="posts.length; else noPosts">
+    <li *ngFor="let post of posts">
+      <a class="entry-link" [routerLink]="['/writing', post.slug]">
+        <span class="entry-tag">{{ post.tag }}</span>
+        <span class="entry-title">{{ post.title }}</span>
+        <p class="entry-summary">{{ post.summary }}</p>
+        <span class="entry-date">{{ post.date }}</span>
+      </a>
+    </li>
+  </ul>
   <ng-template #noPosts>
     <p class="empty-state">Nothing published yet — check back soon.</p>
   </ng-template>
@@ -21,8 +33,6 @@
     </li>
   </ul>
   <ng-template #noLinkedIn>
-    <!-- <p class="empty-state">LinkedIn posts will show up here once added.</p>
-      -->
     <iframe src="https://widgets.sociablekit.com/linkedin-profile-posts/iframe/25696146" frameborder="0" width="100%" height="1000px"></iframe>
   </ng-template>
 </div>
diff --git a/src/app/components/writing-page/writing-page.ts b/src/app/components/writing-page/writing-page.ts
index 07a52e0..0f70c21 100644
--- a/src/app/components/writing-page/writing-page.ts
+++ b/src/app/components/writing-page/writing-page.ts
@@ -1,15 +1,9 @@
 import { Component } from '@angular/core';
 import { CommonModule } from '@angular/common';
+import { RouterLink } from '@angular/router';
 import { Divider } from '../divider/divider';
 import { RevealDirective } from '../../shared/reveal.directive';
-
-interface WritingEntry {
-  title: string;
-  summary: string;
-  date: string;
-  tag: 'Blog' | 'Research';
-  link: string;
-}
+import { BLOG_POSTS } from '../../shared/blog-posts.data';
 
 interface LinkedInEntry {
   caption: string;
@@ -19,19 +13,15 @@ interface LinkedInEntry {
 
 @Component({
   selector: 'app-writing-page',
-  imports: [CommonModule, Divider, RevealDirective],
+  imports: [CommonModule, RouterLink, Divider, RevealDirective],
   templateUrl: './writing-page.html',
   styleUrl: './writing-page.css',
 })
 export class WritingPage {
-  // Add real posts here as you publish them — each becomes a card below.
-  // Example:
-  // { title: 'Why I switched to zoneless Angular', summary: '...', date: 'Jul 2026', tag: 'Blog', link: 'https://...' }
-  protected readonly posts: WritingEntry[] = [];
+  // Posts live in shared/blog-posts.data.ts — add real ones there and they
+  // show up here automatically, each opening on its own page on this site.
+  protected readonly posts = BLOG_POSTS;
 
   // Add links to your own LinkedIn posts here — caption is your own one-line summary.
-  // Example: { caption: 'Shared my thoughts on...', date: 'Jul 2026', url: 'https://www.linkedin.com/posts/...' }
-  protected readonly linkedinPosts: LinkedInEntry[] = [
-   
-  ];
+  protected readonly linkedinPosts: LinkedInEntry[] = [];
 }
diff --git a/src/app/shared/blog-posts.data.ts b/src/app/shared/blog-posts.data.ts
new file mode 100644
index 0000000..7b97941
--- /dev/null
+++ b/src/app/shared/blog-posts.data.ts
@@ -0,0 +1,26 @@
+export interface BlogPost {
+  slug: string;
+  title: string;
+  summary: string;
+  date: string;
+  tag: 'Blog' | 'Research';
+  content: string[];
+}
+
+// Add your real posts here. Each entry automatically gets its own page at
+// /writing/<slug> and shows up in the list on the Writing page —
+// no routing or template changes needed, just add an object below.
+export const BLOG_POSTS: BlogPost[] = [
+  {
+    slug: 'sample-post',
+    title: 'Sample post — replace or delete me',
+    summary:
+      'A placeholder showing how a post looks once it has its own page on the site. Swap this for real writing whenever you\'re ready.',
+    date: 'Jul 2026',
+    tag: 'Blog',
+    content: [
+      'This paragraph is placeholder text. Everything in this post\'s "content" array renders as its own paragraph on the post page below.',
+      'Delete this object from BLOG_POSTS once you have a real post to publish, or just edit it directly in place.',
+    ],
+  },
+];
