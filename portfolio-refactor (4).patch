d  iff --git a/src/app/app.css b/src/app/app.css
index e69de29..c8222e5 100644
--- a/src/app/app.css
+++ b/src/app/app.css
@@ -0,0 +1,81 @@
+.loader {
+  position: fixed;
+  inset: 0;
+  z-index: 10000;
+  display: flex;
+  align-items: center;
+  justify-content: center;
+  background: var(--bg);
+  transition: opacity 500ms ease, visibility 500ms ease;
+}
+
+.loader--hidden {
+  opacity: 0;
+  visibility: hidden;
+  pointer-events: none;
+}
+
+.loader-mark {
+  display: flex;
+  flex-direction: column;
+  align-items: center;
+  gap: 10px;
+}
+
+.loader-mark span {
+  font-size: 12px;
+  letter-spacing: 0.35em;
+  text-transform: uppercase;
+  color: var(--fg);
+  opacity: 0;
+  animation: loader-fade 600ms ease forwards 150ms;
+}
+
+.loader-line {
+  display: block;
+  width: 64px;
+  height: 1px;
+  background: var(--accent);
+  transform: scaleX(0);
+  transform-origin: left;
+  animation: loader-draw 900ms cubic-bezier(0.16, 0.84, 0.44, 1) forwards 250ms;
+}
+
+@keyframes loader-fade {
+  to {
+    opacity: 1;
+  }
+}
+
+@keyframes loader-draw {
+  to {
+    transform: scaleX(1);
+  }
+}
+
+.site {
+  opacity: 0;
+  transform: translateY(8px);
+  transition: opacity 600ms ease, transform 600ms ease;
+}
+
+.site--visible {
+  opacity: 1;
+  transform: none;
+}
+
+@media (prefers-reduced-motion: reduce) {
+  .loader-mark span,
+  .loader-line {
+    animation: none;
+    opacity: 1;
+    transform: none;
+  }
+}
+
+@media (max-width: 360px) {
+  .loader-mark span {
+    font-size: 10px;
+    letter-spacing: 0.22em;
+  }
+}
diff --git a/src/app/app.html b/src/app/app.html
index 2c42064..b04f2ab 100644
--- a/src/app/app.html
+++ b/src/app/app.html
@@ -1,2 +1,11 @@
-<app-landing-page></app-landing-page>
-<app-hover-card></app-hover-card>
\ No newline at end of file
+<div class="loader" [class.loader--hidden]="!loading()" aria-hidden="true">
+  <div class="loader-mark">
+    <span>Ankush Shukla</span>
+    <i class="loader-line"></i>
+  </div>
+</div>
+
+<div class="site" [class.site--visible]="!loading()">
+  <app-landing-page></app-landing-page>
+  <app-hover-card></app-hover-card>
+</div>
diff --git a/src/app/app.ts b/src/app/app.ts
index b6b3346..e80fbb8 100644
--- a/src/app/app.ts
+++ b/src/app/app.ts
@@ -1,4 +1,4 @@
-import { Component, signal } from '@angular/core';
+import { Component, OnInit, signal } from '@angular/core';
 import { LandingPage } from './components/landing-page/landing-page';
 import { HoverCard } from './components/hover-card/hover-card';
 
@@ -8,7 +8,13 @@ import { HoverCard } from './components/hover-card/hover-card';
   templateUrl: './app.html',
   styleUrl: './app.css',
 })
-export class App {
+export class App implements OnInit {
   protected readonly title = signal('portfolioWebsite');
-}
+  protected readonly loading = signal(true);
 
+  ngOnInit(): void {
+    const prefersReducedMotion =
+      typeof window.matchMedia === 'function' && window.matchMedia('(prefers-reduced-motion: reduce)').matches;
+    window.setTimeout(() => this.loading.set(false), prefersReducedMotion ? 0 : 1100);
+  }
+}
diff --git a/src/app/components/about-me/about-me.css b/src/app/components/about-me/about-me.css
index a772b50..6a127b0 100644
--- a/src/app/components/about-me/about-me.css
+++ b/src/app/components/about-me/about-me.css
@@ -1,3 +1,8 @@
 .tags{
     color : #5e5c5c;
 }
+
+.outro{
+    color: var(--muted);
+    font-size: 0.95em;
+}
diff --git a/src/app/components/about-me/about-me.html b/src/app/components/about-me/about-me.html
index 68c675c..2a3caff 100644
--- a/src/app/components/about-me/about-me.html
+++ b/src/app/components/about-me/about-me.html
@@ -1,22 +1,31 @@
 <div class="main-container">
   <p class="aboutMe personal">
     Hi, I'm Ankush Shukla, a MCA Student & BCA graduate who enjoys turning ideas into practical and impactful
-    digital experiences. I am driven by curiosity, continuous learning, and the challenge of solving
-    complex problems through technology.
+    digital experiences. I am driven by
+    <span class="spark" tabindex="0" appHoverCard [hoverCard]="wordMeanings['curiosity']">curiosity</span>, continuous
+    <span class="spark" tabindex="0" appHoverCard [hoverCard]="wordMeanings['learning']">learning</span>, and the challenge of solving
+    <span class="spark" tabindex="0" appHoverCard [hoverCard]="wordMeanings['complexProblems']">complex problems</span> through technology.
   </p>
   <br />
 
   <p class="aboutMe work">
     My work focuses on building thoughtful solutions, improving user experiences, and approaching
-    challenges with a problem-solving mindset. I enjoy exploring new domains, learning emerging
+    challenges with a <span class="spark" tabindex="0" appHoverCard [hoverCard]="wordMeanings['problemSolvingMindset']">problem-solving mindset</span>. I enjoy exploring new domains, learning emerging
     technologies, and expanding my skills through hands-on experimentation.
   </p>
   <br />
 
   <p class="aboutMe believes">
-    I believe growth comes from building, learning, and sharing knowledge. I'm always interested in
-    connecting with people who are passionate about technology, innovation, and creating meaningful
-    products.
+    I believe <span class="spark" tabindex="0" appHoverCard [hoverCard]="wordMeanings['growth']">growth</span> comes from building, learning, and sharing knowledge. I'm always interested in
+    connecting with people who are passionate about technology, innovation, and creating
+    <span class="spark" tabindex="0" appHoverCard [hoverCard]="wordMeanings['meaningfulProducts']">meaningful products</span>.
   </p>
   <br />
+
+  <p class="aboutMe outro">
+    Always open to interesting conversations about software, problem-solving, and side projects. Say hello or
+    follow me on <a class="link-draw" href="https://github.com/Ankush-Shukla" target="_blank" rel="noopener">GitHub</a>,
+    <a class="link-draw" href="https://www.linkedin.com/in/ankush-shukla/" target="_blank" rel="noopener">LinkedIn</a>, or
+    <a class="link-draw" href="https://www.instagram.com/ankush.shukla__/" target="_blank" rel="noopener">Instagram</a>.
+  </p>
 </div>
diff --git a/src/app/components/about-me/about-me.ts b/src/app/components/about-me/about-me.ts
index e143946..7b00c61 100644
--- a/src/app/components/about-me/about-me.ts
+++ b/src/app/components/about-me/about-me.ts
@@ -1,9 +1,44 @@
 import { Component } from '@angular/core';
+import { HoverCardDirective } from '../hover-card/hover-card.directive';
+import { HoverCardData } from '../hover-card/hover-card.model';
 
 @Component({
   selector: 'app-about-me',
-  imports: [],
+  imports: [HoverCardDirective],
   templateUrl: './about-me.html',
   styleUrl: './about-me.css',
 })
-export class AboutMe {}
+export class AboutMe {
+  protected readonly wordMeanings: Record<string, HoverCardData> = {
+    curiosity: {
+      title: 'Curiosity',
+      subtitle: 'noun',
+      description: 'A strong desire to explore, question, and understand how things work.',
+    },
+    learning: {
+      title: 'Learning',
+      subtitle: 'noun',
+      description: 'The ongoing process of gaining new knowledge or skills through study and practice.',
+    },
+    complexProblems: {
+      title: 'Complex problems',
+      subtitle: 'phrase',
+      description: 'Challenges made of many interconnected parts, with no single obvious solution.',
+    },
+    problemSolvingMindset: {
+      title: 'Problem-solving mindset',
+      subtitle: 'phrase',
+      description: 'An approach centered on breaking challenges down and finding practical, workable solutions.',
+    },
+    growth: {
+      title: 'Growth',
+      subtitle: 'noun',
+      description: 'Steady progress in skill, understanding, or capability over time.',
+    },
+    meaningfulProducts: {
+      title: 'Meaningful products',
+      subtitle: 'phrase',
+      description: 'Things built to genuinely help or improve the lives of the people who use them.',
+    },
+  };
+}
diff --git a/src/app/components/contact/contact.css b/src/app/components/contact/contact.css
index d638338..f0f9c08 100644
--- a/src/app/components/contact/contact.css
+++ b/src/app/components/contact/contact.css
@@ -7,6 +7,7 @@ textarea {
     outline-width: 1px;
     outline-style:dotted;
     padding : 8px;
+    transition: outline-color 300ms ease, box-shadow 300ms ease;
 }
 
 input:focus,
@@ -16,6 +17,9 @@ textarea:active {
     border: none !important;
     /* outline: none !important; */
     background-color: #171717;
+    outline-color: var(--accent);
+    outline-style: solid;
+    box-shadow: 0 0 0 3px var(--accent-soft);
 }
 .form-group{
     display: grid;
@@ -25,6 +29,14 @@ textarea:active {
     align-items: center;
 }
 
+@media (max-width: 480px) {
+  .form-group{
+    grid-template-columns: 1fr;
+    gap: 6px;
+    margin: 16px 4px;
+  }
+}
+
 .contact-section{
     display: flex;
     flex-direction: row;
@@ -32,9 +44,47 @@ textarea:active {
 }
 
 button{
-     display:flex;
+    display:flex;
     gap: 16px;
     margin:16px;
+    position: relative;
+    overflow: hidden;
+    padding: 10px 20px;
+    border-radius: 8px;
+    border: 1px solid rgba(94, 91, 79, 0.5);
+    background: transparent;
+    color: var(--fg);
+    font-family: inherit;
+    transition: color 400ms var(--ease-out), border-color 400ms var(--ease-out);
+}
+
+button::before{
+    content: '';
+    position: absolute;
+    inset: 0;
+    background: var(--accent);
+    transform: scaleX(0);
+    transform-origin: left;
+    transition: transform 450ms var(--ease-out);
+    z-index: 0;
+}
+
+button span{
+    position: relative;
+    z-index: 1;
+    transition: color 400ms var(--ease-out);
+}
+
+button:hover{
+    border-color: var(--accent);
+}
+
+button:hover::before{
+    transform: scaleX(1);
+}
+
+button:hover span{
+    color: #12120D;
 }
 
 /* Ensure inputs take available space and align consistently */
diff --git a/src/app/components/contact/contact.html b/src/app/components/contact/contact.html
index ccb872d..07ad7b3 100644
--- a/src/app/components/contact/contact.html
+++ b/src/app/components/contact/contact.html
@@ -17,6 +17,6 @@
       <textarea id="message" rows="2" #messageInput placeholder="Tell me about your project" required></textarea>
     </div>
 
-    <button type="submit">Send message !</button>
+    <button type="submit"><span>Send message !</span></button>
   </form>
 </section>
diff --git a/src/app/components/divider/divider.css b/src/app/components/divider/divider.css
index 02a578b..0e340a3 100644
--- a/src/app/components/divider/divider.css
+++ b/src/app/components/divider/divider.css
@@ -14,4 +14,15 @@
 .divider span {
   font-weight: 200;
   white-space: nowrap;
+}
+
+@media (max-width: 480px) {
+  .divider {
+    gap: 0.6rem;
+    margin: 16px 0px 16px 0px;
+  }
+
+  .divider span {
+    font-size: 0.85rem;
+  }
 }
\ No newline at end of file
diff --git a/src/app/components/footer/footer.css b/src/app/components/footer/footer.css
index b8944a8..1c577f6 100644
--- a/src/app/components/footer/footer.css
+++ b/src/app/components/footer/footer.css
@@ -1,6 +1,7 @@
 .socials{
     display: flex;
     flex-direction: row;
+    flex-wrap: wrap;
     justify-content: center;
     align-items: center;
     gap : 10px;
diff --git a/src/app/components/footer/footer.html b/src/app/components/footer/footer.html
index b4cab44..b766bb0 100644
--- a/src/app/components/footer/footer.html
+++ b/src/app/components/footer/footer.html
@@ -5,9 +5,9 @@
         <p class="quote">"Never regret thy fall, O Icarus of the fearless flight, For the greatest tragedy of them all Is never to feel the burning light."</p>
     </div>
     <ul class="socials">
-        <li><a href="">Github</a></li>
-        <li><a href="">LinkedIn</a></li>
-        <li><a href="">X</a></li>
-        <li><a href="">Instagram</a></li>
+        <li><a class="link-draw" href="" target="_blank" rel="noopener">Github</a></li>
+        <li><a class="link-draw" href="" target="_blank" rel="noopener">LinkedIn</a></li>
+        <li><a class="link-draw" href="" target="_blank" rel="noopener">X</a></li>
+        <li><a class="link-draw" href="" target="_blank" rel="noopener">Instagram</a></li>
     </ul>
 </div>
\ No newline at end of file
diff --git a/src/app/components/header/header.css b/src/app/components/header/header.css
index 3dcf616..bc76d40 100644
--- a/src/app/components/header/header.css
+++ b/src/app/components/header/header.css
@@ -13,6 +13,13 @@
     border-radius: 8px;
     margin: 4px;
     justify-content: space-around;
+    transition: transform 400ms var(--ease-out), box-shadow 400ms var(--ease-out);
+    flex-shrink: 0;
+}
+
+.profile-pic:hover{
+    transform: rotate(-4deg) scale(1.08);
+    box-shadow: 0 8px 24px rgba(232, 163, 61, 0.25);
 }
 
 .header-container{
@@ -20,15 +27,33 @@
     padding:8px 8px 8px 0px ;
     display: flex;
     flex-direction: row;
+    flex-wrap: wrap;
 }
 .name-tagline{
     display: flex;
     flex-direction: row;
     align-items: center;
     justify-content: space-between;
+    flex-wrap: wrap;
     margin:4px;
+    min-width: 0;
 }
 
 .tags{
     margin:3px 3px 3px 10px;
+}
+
+@media (max-width: 480px) {
+  .name{
+    font-size: 24px;
+  }
+
+  .name-tagline{
+    flex-direction: column;
+    align-items: flex-start;
+  }
+
+  .tags{
+    margin: 4px 3px 3px 0;
+  }
 }
\ No newline at end of file
diff --git a/src/app/components/hover-card/hover-card.css b/src/app/components/hover-card/hover-card.css
index 435fc0c..e2cb30a 100644
--- a/src/app/components/hover-card/hover-card.css
+++ b/src/app/components/hover-card/hover-card.css
@@ -4,16 +4,16 @@
   max-width: 320px;
   width: min(100vw - 24px, 320px);
   padding: 1rem;
-  border-radius: 20px;
-  background: rgba(18, 20, 26, 0.96);
-  color: #f8fafc;
-  box-shadow: 0 28px 70px rgba(0, 0, 0, 0.22);
+  border-radius: 14px;
+  background: var(--surface);
+  color: var(--fg);
+  box-shadow: 0 24px 60px rgba(0, 0, 0, 0.45);
   pointer-events: none;
   opacity: 0;
   transform: translateY(8px) scale(0.98);
   transition: opacity 180ms ease, transform 180ms ease;
   line-height: 1.5;
-  border: 1px solid rgba(255, 255, 255, 0.06);
+  border: 1px solid rgba(232, 163, 61, 0.18);
 }
 
 .hover-card.visible {
@@ -32,26 +32,30 @@
   margin: 0;
   font-size: 1rem;
   font-weight: 700;
+  color: var(--fg);
 }
 
 .hover-card-subtitle {
   margin: 0.3rem 0 0;
-  color: #94a3b8;
-  font-size: 0.86rem;
+  color: var(--accent);
+  font-size: 0.78rem;
+  letter-spacing: 0.03em;
+  text-transform: uppercase;
 }
 
 .hover-card-image {
   width: 48px;
   height: 48px;
-  border-radius: 14px;
+  border-radius: 12px;
   object-fit: cover;
   flex-shrink: 0;
 }
 
 .hover-card-description {
   margin: 0;
-  color: #d1d5db;
-  font-size: 0.95rem;
+  color: var(--fg);
+  opacity: 0.85;
+  font-size: 0.92rem;
 }
 
 .hover-card-tags {
@@ -65,15 +69,17 @@
   display: inline-flex;
   align-items: center;
   justify-content: center;
-  padding: 0.35rem 0.75rem;
+  padding: 0.3rem 0.7rem;
   border-radius: 999px;
-  background: rgba(148, 163, 184, 0.14);
-  color: #cbd5e1;
-  font-size: 0.78rem;
+  background: rgba(232, 163, 61, 0.12);
+  color: var(--accent);
+  font-size: 0.75rem;
 }
 
 .hover-card-footer {
   margin: 0.9rem 0 0;
-  font-size: 0.82rem;
-  color: #94a3b8;
+  font-size: 0.78rem;
+  color: var(--muted);
+  border-top: 1px solid rgba(255, 255, 255, 0.06);
+  padding-top: 0.6rem;
 }
diff --git a/src/app/components/hover-card/hover-card.html b/src/app/components/hover-card/hover-card.html
index 140487b..da78121 100644
--- a/src/app/components/hover-card/hover-card.html
+++ b/src/app/components/hover-card/hover-card.html
@@ -1,11 +1,11 @@
-<ng-container *ngIf="state$ | async as state">
+<ng-container *ngIf="state() as state">
   <div
     #card
     class="hover-card"
     role="tooltip"
     [class.visible]="state.visible"
-    [style.left.px]="adjustedLeft"
-    [style.top.px]="adjustedTop"
+    [style.left.px]="adjustedLeft()"
+    [style.top.px]="adjustedTop()"
     *ngIf="state.visible"
   >
     <ng-container *ngIf="state.template; else defaultCard">
diff --git a/src/app/components/hover-card/hover-card.service.ts b/src/app/components/hover-card/hover-card.service.ts
index de0390c..4027867 100644
--- a/src/app/components/hover-card/hover-card.service.ts
+++ b/src/app/components/hover-card/hover-card.service.ts
@@ -1,5 +1,4 @@
-import { Injectable, TemplateRef } from '@angular/core';
-import { BehaviorSubject } from 'rxjs';
+import { Injectable, TemplateRef, signal } from '@angular/core';
 import { HoverCardData, HoverCardState } from './hover-card.model';
 
 const initialState: HoverCardState = {
@@ -12,46 +11,41 @@ const initialState: HoverCardState = {
   providedIn: 'root',
 })
 export class HoverCardService {
-  private stateSubject = new BehaviorSubject<HoverCardState>(initialState);
-  readonly state$ = this.stateSubject.asObservable();
+  private readonly _state = signal<HoverCardState>(initialState);
+  readonly state = this._state.asReadonly();
+
   private showTimeout?: ReturnType<typeof setTimeout>;
-  private pendingData?: HoverCardData;
-  private pendingTemplate?: TemplateRef<unknown>;
+  private hideTimeout?: ReturnType<typeof setTimeout>;
 
-  show(data: HoverCardData = {}, template?: TemplateRef<unknown>, delay = 180): void {
+  show(data: HoverCardData = {}, template?: TemplateRef<unknown>, delay = 150): void {
+    this.cancelPendingHide();
     this.cancelPendingShow();
-    this.pendingData = data;
-    this.pendingTemplate = template;
+
+    if (this._state().visible) {
+      // A card is already open — jump straight to the new target, no re-delay or flicker.
+      this._state.update((s) => ({ ...s, visible: true, data, template }));
+      return;
+    }
+
     this.showTimeout = window.setTimeout(() => {
-      const previous = this.stateSubject.value;
-      this.stateSubject.next({
-        ...previous,
-        visible: true,
-        data: this.pendingData,
-        template: this.pendingTemplate,
-      });
+      this._state.update((s) => ({ ...s, visible: true, data, template }));
       this.showTimeout = undefined;
     }, delay);
   }
 
-  hide(): void {
+  hide(delay = 60): void {
     this.cancelPendingShow();
-    this.stateSubject.next({
-      ...this.stateSubject.value,
-      visible: false,
-      data: undefined,
-      template: undefined,
-      loading: false,
-    });
+    this.cancelPendingHide();
+    // Small grace period: if a neighboring element shows a card within this window,
+    // show() above cancels this timeout and the card never actually disappears.
+    this.hideTimeout = window.setTimeout(() => {
+      this._state.update((s) => ({ ...s, visible: false, data: undefined, template: undefined }));
+      this.hideTimeout = undefined;
+    }, delay);
   }
 
   move(x: number, y: number): void {
-    const current = this.stateSubject.value;
-    this.stateSubject.next({
-      ...current,
-      x,
-      y,
-    });
+    this._state.update((s) => ({ ...s, x, y }));
   }
 
   private cancelPendingShow(): void {
@@ -60,4 +54,11 @@ export class HoverCardService {
       this.showTimeout = undefined;
     }
   }
+
+  private cancelPendingHide(): void {
+    if (this.hideTimeout !== undefined) {
+      clearTimeout(this.hideTimeout);
+      this.hideTimeout = undefined;
+    }
+  }
 }
diff --git a/src/app/components/hover-card/hover-card.ts b/src/app/components/hover-card/hover-card.ts
index 689eb43..746d7d9 100644
--- a/src/app/components/hover-card/hover-card.ts
+++ b/src/app/components/hover-card/hover-card.ts
@@ -1,6 +1,5 @@
-import { AfterViewInit, Component, ElementRef, ViewChild } from '@angular/core';
+import { Component, ElementRef, ViewChild, effect, inject, signal } from '@angular/core';
 import { CommonModule } from '@angular/common';
-import { Observable } from 'rxjs';
 import { HoverCardService } from './hover-card.service';
 import { HoverCardState } from './hover-card.model';
 
@@ -11,21 +10,23 @@ import { HoverCardState } from './hover-card.model';
   templateUrl: './hover-card.html',
   styleUrl: './hover-card.css',
 })
-export class HoverCard implements AfterViewInit {
-  state$: Observable<HoverCardState>;
+export class HoverCard {
+  private readonly hoverCardService = inject(HoverCardService);
+
+  protected readonly state = this.hoverCardService.state;
 
   @ViewChild('card', { static: false }) cardRef?: ElementRef<HTMLElement>;
 
-  adjustedLeft = 0;
-  adjustedTop = 0;
-  private raf?: number;
+  // Signals (not plain fields) so position updates re-render even though they're
+  // written from a requestAnimationFrame callback outside any Angular event —
+  // this app is zoneless, and only signal writes are guaranteed to schedule a re-render.
+  protected readonly adjustedLeft = signal(0);
+  protected readonly adjustedTop = signal(0);
 
-  constructor(private readonly hoverCardService: HoverCardService) {
-    this.state$ = this.hoverCardService.state$;
-  }
+  private raf?: number;
 
-  ngAfterViewInit(): void {
-    this.state$.subscribe((state) => this.updatePosition(state));
+  constructor() {
+    effect(() => this.updatePosition(this.state()));
   }
 
   private updatePosition(state: HoverCardState): void {
@@ -58,8 +59,8 @@ export class HoverCard implements AfterViewInit {
         }
       }
 
-      this.adjustedLeft = Math.max(12, targetLeft);
-      this.adjustedTop = Math.max(12, targetTop);
+      this.adjustedLeft.set(Math.max(12, targetLeft));
+      this.adjustedTop.set(Math.max(12, targetTop));
     });
   }
 }
diff --git a/src/app/components/landing-page/landing-page.html b/src/app/components/landing-page/landing-page.html
index 23a9858..1d34ca5 100644
--- a/src/app/components/landing-page/landing-page.html
+++ b/src/app/components/landing-page/landing-page.html
@@ -1,15 +1,15 @@
 <!-- <app-nav-bar></app-nav-bar> -->
 
-<div class="intro">
+<div class="intro" appReveal>
     <app-divider [title]="'Hello There!'" />
     <app-header></app-header>
     <app-about-me></app-about-me>
 </div>
-<app-divider [title]="'Things I have worked '" />
-<app-my-work></app-my-work>
-<app-divider [title]="'Stuff I know about'" />
-<app-my-skills></app-my-skills>
-<app-divider [title]="'Let\'s Talk'" />
-<app-contact></app-contact>
-<app-divider [title]="'Thanks for visiting :)'" />
-<app-footer></app-footer>
\ No newline at end of file
+<app-divider [title]="'Things I have worked '" appReveal />
+<app-my-work appReveal></app-my-work>
+<app-divider [title]="'Stuff I know about'" appReveal />
+<app-my-skills appReveal></app-my-skills>
+<app-divider [title]="'Let\'s Talk'" appReveal />
+<app-contact appReveal></app-contact>
+<app-divider [title]="'Thanks for visiting :)'" appReveal />
+<app-footer appReveal></app-footer>
diff --git a/src/app/components/landing-page/landing-page.ts b/src/app/components/landing-page/landing-page.ts
index fb141e8..af2a73a 100644
--- a/src/app/components/landing-page/landing-page.ts
+++ b/src/app/components/landing-page/landing-page.ts
@@ -7,10 +7,11 @@ import { MySkills } from '../my-skills/my-skills';
 import { Contact } from '../contact/contact';
 import { Footer } from '../footer/footer';
 import { Divider } from '../divider/divider';
+import { RevealDirective } from '../../shared/reveal.directive';
 
 @Component({
   selector: 'app-landing-page',
-  imports: [AboutMe,NavBar,MyWork,Header,MySkills,Contact,Footer,Divider],
+  imports: [AboutMe,NavBar,MyWork,Header,MySkills,Contact,Footer,Divider,RevealDirective],
   templateUrl: './landing-page.html',
   styleUrl: './landing-page.css',
 })
diff --git a/src/app/components/my-skills/my-skills.css b/src/app/components/my-skills/my-skills.css
index 410fbb7..ca82066 100644
--- a/src/app/components/my-skills/my-skills.css
+++ b/src/app/components/my-skills/my-skills.css
@@ -5,6 +5,15 @@
   
 }
 
+.logo-item img{
+    transition: transform 350ms var(--ease-out);
+}
+
+.logo-item:hover img,
+.logo-item:focus-within img{
+    transform: translateY(-4px) scale(1.12);
+}
+
 .logos-grid{
     gap:16px;
 }
\ No newline at end of file
diff --git a/src/app/components/my-skills/my-skills.html b/src/app/components/my-skills/my-skills.html
index 15d5c71..978ef44 100644
--- a/src/app/components/my-skills/my-skills.html
+++ b/src/app/components/my-skills/my-skills.html
@@ -1,5 +1,5 @@
 <section class="skills">
-  <div class="logos-grid grid grid-cols-6 gap-4">
+  <div class="logos-grid grid grid-cols-4 sm:grid-cols-6 gap-4">
     <ng-container *ngFor="let skill of skills">
       <figure class="logo-item" appHoverCard [hoverCard]="skill.hoverCard">
         <img class="h-8" [src]="skill.path" [alt]="skill.name" loading="lazy" />
diff --git a/src/app/components/my-work/my-work.css b/src/app/components/my-work/my-work.css
index 470b601..9a06094 100644
--- a/src/app/components/my-work/my-work.css
+++ b/src/app/components/my-work/my-work.css
@@ -1,8 +1,33 @@
 span{
     font-weight: 600;
+    transition: color 350ms var(--ease-out);
 }
 
 ul{
     display: grid;
     gap:24px;
+}
+
+.projects-list a{
+    text-decoration: none;
+    color: inherit;
+    display: block;
+}
+
+.projects-list li{
+    padding: 14px 16px;
+    margin: -14px -16px;
+    border-radius: 12px;
+    border: 1px solid transparent;
+    transition: border-color 350ms var(--ease-out), background-color 350ms var(--ease-out), transform 350ms var(--ease-out);
+}
+
+.projects-list li:hover{
+    border-color: var(--accent-soft);
+    background-color: rgba(255, 255, 255, 0.03);
+    transform: translateX(6px);
+}
+
+.projects-list li:hover span{
+    color: var(--accent);
 }
\ No newline at end of file
diff --git a/src/app/shared/reveal.directive.ts b/src/app/shared/reveal.directive.ts
new file mode 100644
index 0000000..3cf1df6
--- /dev/null
+++ b/src/app/shared/reveal.directive.ts
@@ -0,0 +1,51 @@
+import { AfterViewInit, Directive, ElementRef, Input, OnDestroy } from '@angular/core';
+
+/**
+ * Fades + lifts an element into place the first time it scrolls into view.
+ * Usage: <section appReveal></section>  or  <section appReveal [revealDelay]="120"></section>
+ */
+@Directive({
+  selector: '[appReveal]',
+  standalone: true,
+})
+export class RevealDirective implements AfterViewInit, OnDestroy {
+  @Input() revealDelay = 0;
+
+  private observer?: IntersectionObserver;
+
+  constructor(private readonly el: ElementRef<HTMLElement>) {}
+
+  ngAfterViewInit(): void {
+    const node = this.el.nativeElement;
+    node.classList.add('reveal');
+
+    if (this.revealDelay) {
+      node.style.transitionDelay = `${this.revealDelay}ms`;
+    }
+
+    const prefersReducedMotion =
+      typeof window.matchMedia === 'function' && window.matchMedia('(prefers-reduced-motion: reduce)').matches;
+    if (prefersReducedMotion || !('IntersectionObserver' in window)) {
+      node.classList.add('revealed');
+      return;
+    }
+
+    this.observer = new IntersectionObserver(
+      (entries) => {
+        for (const entry of entries) {
+          if (entry.isIntersecting) {
+            node.classList.add('revealed');
+            this.observer?.unobserve(node);
+          }
+        }
+      },
+      { threshold: 0.15, rootMargin: '0px 0px -60px 0px' },
+    );
+
+    this.observer.observe(node);
+  }
+
+  ngOnDestroy(): void {
+    this.observer?.disconnect();
+  }
+}
diff --git a/src/styles.css b/src/styles.css
index 56022a8..dd2fffa 100644
--- a/src/styles.css
+++ b/src/styles.css
@@ -1,7 +1,17 @@
 /* You can add global styles to this file, and also import other style files */
 
-@import 'tailwindcss';
 @import url('https://fonts.googleapis.com/css2?family=Geist:ital,wght@0,100..900;1,100..900&display=swap');
+@import 'tailwindcss';
+
+:root {
+  --bg: #12120d;
+  --fg: #dcdcd8;
+  --muted: #5e5c5c;
+  --surface: #171717;
+  --accent: #e8a33d;
+  --accent-soft: rgba(232, 163, 61, 0.35);
+  --ease-out: cubic-bezier(0.16, 0.84, 0.44, 1);
+}
 
 *{
   margin: 0;
@@ -11,12 +21,100 @@
 }
 body {
   margin: 0 auto;
-  background-color: #12120D;
+  background-color: var(--bg);
   font-family: "Geist";
   font-weight: 250;
-  color: #DCDCD8;
-  max-width: 40%;
-  width: min(100%, 650px);
+  color: var(--fg);
+  width: 100%;
+  max-width: 650px;
+  padding-inline: 24px;
+  box-sizing: border-box;
+}
+
+@media (max-width: 480px) {
+  body {
+    padding-inline: 16px;
+  }
+}
+
+/* ---------- scroll reveal ---------- */
+.reveal {
+  opacity: 0;
+  transform: translateY(28px);
+  transition: opacity 800ms var(--ease-out), transform 800ms var(--ease-out);
+  will-change: opacity, transform;
+}
+.reveal.revealed {
+  opacity: 1;
+  transform: none;
+}
+.reveal hr {
+  transform: scaleX(0);
+  transform-origin: center;
+  transition: transform 900ms var(--ease-out) 120ms;
+}
+.reveal.revealed hr {
+  transform: scaleX(1);
+}
+
+/* ---------- hover "spark" for keywords ---------- */
+.spark {
+  position: relative;
+  color: inherit;
+  cursor: help;
+  background-image: linear-gradient(var(--accent), var(--accent));
+  background-repeat: no-repeat;
+  background-position: left bottom;
+  background-size: 0% 1px;
+  transition: background-size 450ms var(--ease-out), color 450ms var(--ease-out), text-shadow 450ms var(--ease-out);
+  border-radius: 2px;
+}
+.spark:hover,
+.spark:focus-visible {
+  color: #f3d9a4;
+  background-size: 100% 1px;
+  text-shadow: 0 0 18px var(--accent-soft);
+}
+.spark:focus-visible {
+  outline: 1px solid var(--accent-soft);
+  outline-offset: 3px;
+}
+
+/* ---------- underline-draw link style, reused by footer/work links ---------- */
+.link-draw {
+  position: relative;
+  text-decoration: none;
+  color: inherit;
+  transition: color 300ms ease;
+}
+.link-draw::after {
+  content: '';
+  position: absolute;
+  left: 0;
+  bottom: -2px;
+  width: 0%;
+  height: 1px;
+  background: var(--accent);
+  transition: width 350ms var(--ease-out);
+}
+.link-draw:hover,
+.link-draw:focus-visible {
+  color: #f3d9a4;
+}
+.link-draw:hover::after,
+.link-draw:focus-visible::after {
+  width: 100%;
+}
+
+@media (prefers-reduced-motion: reduce) {
+  *,
+  *::before,
+  *::after {
+    animation-duration: 0.01ms !important;
+    animation-iteration-count: 1 !important;
+    transition-duration: 0.01ms !important;
+    scroll-behavior: auto !important;
+  }
 }
 
 
